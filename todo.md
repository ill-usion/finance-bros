1. In onboarding, and after processing the bank statement, display a daily spendings graph over time including the bank statement and forecasted spendings. Make sure to denote both in different colors to make them recognizable.
2. In onboarding, display the recommended weekly budget and give the user the option to use it or not (replaces what the user already specified).
3. In the home page, more specifically in "CATEGORIES" section, remove the progress bars and only display how much the user has spent in each category.
4. In the home page, and underneath "CATEGORIES" section, add a total daily spending graph over time with the past week of spending in addition to the predicted spending. Make sure to denote both in different colors to make them differentiable. Use `/forecast-spending` endpoint and pass the past 28 days of transaction, if there are less than 28 days, pad the missing days with zeros. Only display the past week, and the next week's prediction. Save the predictions locally and allow the user to press a "reload" icon button next to the section title to request a new forecast.
5. When adding a new spending by "Scan receipt" and "From gallery", show a loading icon while the request is being processed.
6. Under the first secion showing the total spending, add a "Financial Analyzer" section showcasing the score and score description, the same thing as present in onboarding. Add a "reload" icon button next to the section title to allow the user to request a new analysis on demand.